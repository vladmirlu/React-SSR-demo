import React from "react";

const HomePage = () => {
  return <div>
  <div>
       This is the Home Page
       </div>
       <button onClick={() => console.log('Is clicked!')}>Click me!</button>
  </div>;
};

export default HomePage;